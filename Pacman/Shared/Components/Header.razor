<div className="header">
    <span className="left title">PACMAN</span>
    <span className="right score">SCORE: <span className="points">@GetPoints()</span></span>
</div>

@code {
    long Points { get; set; }
    bool Playing { get; set; } = true;

    string GetPoints()
    {
        return Playing ? Points.ToString() : "GAME OVER";
    }

    public void Increase()
    {
        Points++;
    }

    public void GameOver()
    {
        Playing = false;
    }
}
